name: Supabase migrations

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**'
  workflow_dispatch:
    inputs:
      run_seed:
        description: 'Also run seed.sql (safe to re-run; orgs+providers are idempotent)'
        type: boolean
        default: false

jobs:
  migrate:
    name: Apply migrations
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      PROJECT_REF: mmvjptmuazcamuduhyzz

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      # Supabase CLI v2.10x removed --project-ref from db commands; they
      # operate on the linked project instead.
      - name: Link project
        run: supabase link --project-ref $PROJECT_REF

      - name: Push migrations
        run: supabase db push --linked

      - name: Run seed
        if: github.event_name == 'workflow_dispatch' && inputs.run_seed == true
        run: supabase db query --linked -f supabase/seed.sql
